context:
  version: "1.0.0"

package:
  name: q-kx-printf
  version: ${{ version }}

source:
  path: ..
  filter:
    exclude:
      - recipe/**

build:
  number: 0
  noarch: generic
  script:
    - mkdir -p $PREFIX/lib/q/mod/kx
    - cp printf.q $PREFIX/lib/q/mod/kx/

tests:
  - script:
    - test -f ${PREFIX}/lib/q/mod/kx/printf.q
